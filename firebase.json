{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": [
      {
        "collectionGroup": "tasks",
        "queryScope": "COLLECTION",
        "fields": [
          { "fieldPath": "assignedEmail", "order": "ASCENDING" }
        ]
      }
    ]
  }
}
